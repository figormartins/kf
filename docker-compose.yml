services:
  atk-main-bot:
    build:
      context: .
      dockerfile: ./bot/Dockerfile
    container_name: atk-main-bot
    restart: unless-stopped
    
    # Mount volumes to persist data
    volumes:
      - ./bot_data:/app/bot_data
      - ./bot_screenshots:/app/bot_screenshots
    
    # Environment variables (optional customization)
    environment:
      - PYTHONUNBUFFERED=1
      - HEADLESS=true
    
    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  battlefield-bot:
    build:
      context: .
      dockerfile: ./bot_battlefield/Dockerfile
    container_name: battlefield-bot
    restart: unless-stopped
    
    # Environment variables (optional customization)
    environment:
      - PYTHONUNBUFFERED=1
      - HEADLESS=true